# PromptFlow Environment Variables Example
# 复制此文件为 .env 并填入实际值

# 应用环境
NODE_ENV=production

# 服务端口
PORT=3001

# JWT 密钥 (请使用更安全的随机字符串)
JWT_SECRET=wfdea_super_secret_jwt_key

# OpenAI API 配置
OPENAI_API_KEY=sk-proj-your_openai_api_key_here
OPENAI_MODEL=gpt-4.1-nano

# 数据库配置
# 数据库类型: sqlite 或 postgres
DATABASE_TYPE=sqlite

# SQLite 配置 (当 DATABASE_TYPE=sqlite 时使用)
# 数据库文件会自动创建在 backend/database.sqlite

# PostgreSQL 配置 (当 DATABASE_TYPE=postgres 时使用)
POSTGRES_HOST=192.168.31.205
POSTGRES_PORT=5432
POSTGRES_USER=propmt
POSTGRES_PASSWORD=eM8e6GDi4RH66Zm7
POSTGRES_DB=propmt

# Docker Hub 用户名 (用于 GitHub Actions)
DOCKERHUB_USERNAME=maxazure